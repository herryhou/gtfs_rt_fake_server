## Node.js GTFS-realtime serrver simulation

A fake GTFS-realtime HTTP server, which read data from a file (gtfs_rt_data.js)
and send back to HTTP client

### start the server
``` javascript
npm install
npm start
```

### HTTP client test
```
	npm run client
```